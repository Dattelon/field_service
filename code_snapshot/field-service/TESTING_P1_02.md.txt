# 🧪 ИНСТРУКЦИЯ ПО ТЕСТИРОВАНИЮ P1-02

## Быстрый старт

### 1. Перезапустить админ-бота
```bash
cd C:\ProjectF\field-service
# Остановить текущий процесс (Ctrl+C)
python -m field_service.bots.admin_bot.main
```

### 2. Открыть карточку любого заказа
- В админ-боте перейти в "Очередь заказов"
- Выбрать любой заказ
- Посмотреть карточку

---

## Что проверить

### ✅ Проверка #1: История статусов
**Ожидаемый результат:**
```
📋 История статусов:
  • — → CREATED — 08.10.2025 09:15
    created_by_staff
  • CREATED → SEARCHING — 08.10.2025 09:16
  • SEARCHING → ASSIGNED — 08.10.2025 09:25
```

**Как проверить:**
1. Создать новый заказ через админ-бота
2. Дождаться распределения
3. Открыть карточку
4. ✅ В конце карточки должна быть секция "📋 История статусов"

---

### ✅ Проверка #2: Отклонившие мастера
**Ожидаемый результат:**
```
❌ Отклонили (2):
  • Сидоров Сидор (р.1) — 08.10.2025 09:20
  • Алексеев Алексей (р.1) — 08.10.2025 09:21
```

**Как проверить:**
1. Создать заказ
2. Дождаться предложения мастерам
3. Зайти в мастер-бот от нескольких мастеров
4. Отклонить заказ
5. Открыть карточку в админ-боте
6. ✅ Должна быть секция "❌ Отклонили" со списком

---

### ✅ Проверка #3: Время в пути
**Ожидаемый результат:**
```
🚗 В пути: 08.10.2025 14:05
```

**Как проверить:**
1. Создать и назначить заказ мастеру
2. Мастер нажимает "Еду на объект" (переход в EN_ROUTE)
3. Открыть карточку в админ-боте
4. ✅ Должна появиться строка "🚗 В пути"

---

### ✅ Проверка #4: Длительность работы
**Ожидаемый результат:**
```
⏱ Работа: 08.10.2025 14:30 — 08.10.2025 16:45
```

**Как проверить:**
1. Мастер переходит: EN_ROUTE → WORKING → PAYMENT
2. Открыть карточку в админ-боте
3. ✅ Должна быть строка "⏱ Работа" с диапазоном времени

---

## 🐛 Возможные проблемы

### Проблема: "AttributeError: 'OrderDetail' object has no attribute 'status_history'"
**Решение:** Перезапустить админ-бота (импорты не обновились)

### Проблема: История/мастера не показываются
**Решение:** 
1. Проверить что в БД есть данные в таблицах `order_status_history` и `offers`
2. Проверить логи бота на ошибки

### Проблема: "ImportError: cannot import name 'OrderCard'"
**Решение:** Убедиться что все изменения применены:
```bash
# Проверить синтаксис
python -m py_compile field_service/bots/admin_bot/core/dto.py
python -m py_compile field_service/bots/admin_bot/services/orders.py
python -m py_compile field_service/bots/admin_bot/ui/texts/orders.py
```

---

## 📝 Логи для проверки

Если нужно убедиться что данные загружаются:
```python
# Добавить временно в orders.py для дебага:
logger.info(f"Loaded {len(status_history)} history items for order {order_id}")
logger.info(f"Loaded {len(declined_masters)} declined masters for order {order_id}")
```

---

## ✅ Чек-лист тестирования

- [ ] Админ-бот перезапущен
- [ ] История статусов отображается
- [ ] Отклонившие мастера показываются (если есть)
- [ ] Время в пути показывается (если статус EN_ROUTE был)
- [ ] Длительность работы показывается (если WORKING и PAYMENT были)
- [ ] Карточка не сломана, все поля на месте
- [ ] Нет ошибок в логах бота

---

## 🚀 После успешного тестирования

1. Закоммитить изменения:
```bash
cd C:\ProjectF\field-service
git add .
git commit -m "feat(admin-bot): P1-02 UI improvements - order card enhancement"
```

2. Обновить `CHANGELOG.md`

3. Перейти к следующей задаче: **P1-04: Отчёты по периодам**

---

**Документация:** `P1_02_UI_IMPROVEMENTS_COMPLETE.md`  
**Быстрый старт:** `QUICKSTART_P1_02.md`
