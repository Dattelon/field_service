# P0-2: Редактирование данных онбординга

**Дата:** 2025-10-08  
**Статус:** В разработке  
**Приоритет:** P0 (Критический)

## 🎯 Задача

Добавить возможность редактирования данных на шаге подтверждения онбординга. Сейчас мастер не может исправить ошибки без повторного прохождения всей анкеты.

## 📋 Текущая проблема

```
Мастер заполнил анкету → Увидел опечатку в фамилии → 
Нужно проходить весь онбординг заново (14 шагов!)
```

## ✅ Решение

На шаге `OnboardingStates.confirm` добавить кнопки "✏️ Изменить" для каждого редактируемого поля.

## 🔧 Технические детали

### 1. Флаг редактирования в state

Добавляем в FSM state флаг `is_editing=True`, когда пользователь нажимает кнопку редактирования.

### 2. Кнопки редактирования

Каждое поле в summary получает inline кнопку:
- ФИО → `m:onb:edit:name`
- Телефон → `m:onb:edit:phone`
- Город → `m:onb:edit:city`
- Районы → `m:onb:edit:districts`
- Автомобиль → `m:onb:edit:vehicle`
- Навыки → `m:onb:edit:skills`
- Способ выплаты → `m:onb:edit:payout`
- Дом-база → `m:onb:edit:home_geo`

### 3. Логика возврата

После редактирования поля проверяем флаг `is_editing`:
- Если `True` → возвращаемся к `_show_summary()` 
- Если `False` → продолжаем обычный flow

## 📝 Изменяемые файлы

1. `master_bot/handlers/onboarding.py`
   - Изменить `_show_summary()` - добавить кнопки редактирования
   - Добавить handlers для callback'ов редактирования
   - Модифицировать handlers полей для проверки `is_editing`

## 🎨 UI/UX

### До:
```
Проверьте данные анкеты:
ФИО: Иванов Иван Иванович
Телефон: +79991234567
...

[✅ Отправить]
```

### После:
```
Проверьте данные анкеты:
ФИО: Иванов Иван Иванович [✏️]
Телефон: +79991234567 [✏️]
Город: Москва [✏️]
Районы: Центральный, Северный [✏️]
...

[✅ Отправить на модерацию]
```

## ⚠️ Особенности реализации

1. **ФИО** - редактируем все 3 части последовательно (фамилия → имя → отчество)
2. **Файлы** (паспорт, селфи) - нельзя редактировать (слишком сложно для MVP)
3. **Город** - при смене города сбрасываем районы (нужно выбрать заново)
4. **Флаг is_editing** сбрасывается после возврата к summary

## 🧪 Тестовые сценарии

1. Изменение простого поля (телефон)
2. Изменение города → сброс районов
3. Изменение ФИО → все 3 поля последовательно
4. Отмена редактирования → возврат к summary без изменений

## 📊 Метрики успеха

- % пользователей, использующих редактирование
- Среднее количество редактирований на анкету
- Время заполнения анкеты с учётом редактирования
