# BUGFIX: Устранение блокировок БД в тестах

**Дата:** 2025-10-15  
**Статус:** ✅ RESOLVED  
**Приоритет:** CRITICAL

## 🎯 Проблема

Тесты зависали на 90+ секунд при инициализации фикстуры `async_session`, что указывало на критические блокировки PostgreSQL:

- Обнаружены 3 процесса, ожидающих `AccessExclusiveLock` более 54 минут
- DDL-операции (DROP TYPE, CREATE TYPE, TRUNCATE TABLE) в фикстуре `engine` вызывали взаимоблокировки
- Фикстура `_seed_minimal_reference_data` с `autouse=True` создавала конфликтующие данные
- Схема тестовой БД не соответствовала моделям SQLAlchemy (отсутствовали поля из миграций)

## ✅ Решение

### 1. Упрощение фикстур conftest.py

**Файл:** `tests/conftest.py`

#### 1.1. Фикстура `engine` - убраны DDL-операции

```python
# БЫЛО - вызывало блокировки:
@pytest.fixture(scope="session")
async def engine():
    # ... DDL операции: DROP TYPE, CREATE TYPE, TRUNCATE TABLE
    
# СТАЛО - только создание engine:
@pytest.fixture(scope="session")
async def engine():
    """Создаём engine только для подключения - без изменения схемы."""
    e = create_async_engine(
        DATABASE_URL,
        echo=False,
        poolclass=NullPool,
        future=True,
    )
    yield e
    await e.dispose()
```

**Обоснование:** Схема БД создаётся миграциями Alembic, а не фикстурами. DDL-операции в фикстурах блокировались при наличии активных транзакций.

#### 1.2. Убран `autouse=True` из seed-фикстуры

```python
# БЫЛО - создавало дубликаты:
@pytest.fixture(scope="function", autouse=True)
async def _seed_minimal_reference_data(async_session):
    # ... создание городов, районов, навыков

# СТАЛО - запускается только по запросу:
@pytest.fixture(scope="function")
async def seed_minimal_data(async_session):
    # ... создание городов, районов, навыков
```

**Обоснование:** С `autouse=True` фикстура запускалась для КАЖДОГО теста и создавала конфликтующие данные. Теперь тесты явно запрашивают эту фикстуру при необходимости.

### 2. Применение миграций Alembic к тестовой БД

#### 2.1. Исправление цепочки миграций

**Файл:** `alembic/versions/2025_10_15_0001_seed_ci_minimal.py`

```python
# БЫЛО - ссылалась на несуществующую ревизию:
down_revision = "2025_10_15_add_offers_partial_indexes"

# СТАЛО - правильная ревизия:
down_revision = "2025_10_09_0001"
```

#### 2.2. Создание merge-миграции

Обнаружены два независимых head:
- `2025_10_02_0001` (основная ветка)
- `2025_10_15_0001` (тестовая ветка)

Создана merge-миграция:
```bash
alembic merge -m "merge_heads_test_and_main" 2025_10_02_0001 2025_10_15_0001
# Результат: 4c2465ccb4e5_merge_heads_test_and_main.py
```

#### 2.3. Применение всех миграций

```bash
# Пересоздание тестовой БД
DROP DATABASE field_service_test;
CREATE DATABASE field_service_test OWNER field_user;

# Применение миграций с UTF-8 кодировкой
$env:PYTHONIOENCODING="utf-8"
$env:DATABASE_URL="postgresql+asyncpg://field_user:field_pass@localhost:5439/field_service_test"
$env:PYTHONPATH="C:\ProjectF\field-service"
alembic upgrade head
```

**Результат:** Схема тестовой БД синхронизирована с моделями - все поля существуют.

### 3. Очистка seed-данных из миграций

```sql
DELETE FROM skills;
DELETE FROM districts;
DELETE FROM cities;
```

**Обоснование:** Seed-данные из миграций не участвуют в ROLLBACK и конфликтуют с тестами. Тесты должны создавать свои данные в транзакционном контексте.

### 4. Обновление pytest.ini

**Файл:** `pytest.ini`

```ini
[pytest]
asyncio_mode = strict
timeout = 90
addopts = -ra -q
filterwarnings =
    ignore::DeprecationWarning
```

**Изменения:**
- Добавлен `asyncio_mode = strict` для строгого контроля async-тестов
- Установлен `timeout = 90` для предотвращения зависаний
- Игнорируются DeprecationWarning для чистоты вывода

### 5. Обновление документации тестирования

**Файл:** `docs/testing/TESTING_GUIDELINES.md`

Полностью переписан раздел "Database Management", убраны все упоминания TRUNCATE TABLE, добавлено описание механизма SAVEPOINT + ROLLBACK.

## 📊 Результаты

### До исправления:
```
❌ Тесты зависали на 90+ секунд
❌ Обнаружены блокировки AccessExclusiveLock > 54 минут
❌ Ошибки: "duplicate key value violates unique constraint"
❌ Ошибки: "column orders.cancel_reason does not exist"
```

### После исправления:
```
✅ Тест выполняется за ~4-6 секунд без зависаний
✅ Блокировок БД не обнаружено (pg_stat_activity пуста)
✅ Схема БД синхронизирована с моделями
✅ Механизм ROLLBACK работает корректно
✅ Нет конфликтов при создании данных
```

### Проверка состояния БД:

```sql
-- Активные подключения к тестовой БД
SELECT COUNT(*) FROM pg_stat_activity 
WHERE datname = 'field_service_test';
-- Результат: 0 (чисто!)

-- Блокировки
SELECT COUNT(*) FROM pg_stat_activity 
WHERE wait_event_type IS NOT NULL;
-- Результат: 0 (нет ожиданий!)
```

## 🔍 Ключевые инсайты

1. **DDL-операции в фикстурах опасны** - они требуют эксклюзивных блокировок и конфликтуют с активными транзакциями тестов

2. **autouse=True следует избегать** - автоматический запуск фикстур для каждого теста создаёт непредсказуемые конфликты данных

3. **Миграции > фикстуры для схемы БД** - схема должна создаваться Alembic, а не pytest-фикстурами

4. **Seed-данные должны быть транзакционными** - данные из миграций не откатываются через ROLLBACK и создают конфликты

5. **SAVEPOINT + ROLLBACK достаточно** - не нужны TRUNCATE TABLE, DELETE или DROP/CREATE для очистки между тестами

## 📝 Файлы изменены

- ✏️ `tests/conftest.py` - упрощены фикстуры engine и seed
- ✏️ `pytest.ini` - добавлены настройки asyncio и timeout
- ✏️ `docs/testing/TESTING_GUIDELINES.md` - обновлена документация
- ✏️ `alembic/versions/2025_10_15_0001_seed_ci_minimal.py` - исправлена ревизия
- ➕ `alembic/versions/4c2465ccb4e5_merge_heads_test_and_main.py` - merge миграция

## 🚀 Следующие шаги

1. ✅ Блокировки устранены - тесты выполняются быстро
2. ⚠️ Исправить падающие тесты (логические ошибки в коде, не связанные с БД)
3. 📊 Мониторинг pg_stat_activity при полном прогоне тестов
4. 📚 Документировать паттерн транзакционного тестирования для команды

## 🎓 Best Practices для будущих тестов

```python
# ✅ ПРАВИЛЬНО - явный запрос фикстур
@pytest.mark.asyncio
async def test_something(async_session, seed_minimal_data):
    city = m.cities(name="My City", ...)
    async_session.add(city)
    await async_session.flush()
    # ... тест ...
    # ROLLBACK происходит автоматически

# ❌ НЕПРАВИЛЬНО - DDL в тестах
@pytest.mark.asyncio
async def test_something(async_session):
    await async_session.execute(text("TRUNCATE cities CASCADE"))
    # ... тест ...
```

---

**Автор:** Claude (Desktop Commander)  
**Дата завершения:** 2025-10-15 13:30 UTC
