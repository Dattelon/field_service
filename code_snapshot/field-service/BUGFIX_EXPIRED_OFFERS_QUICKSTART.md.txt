 SQL запрос:
```sql
WHERE m.offers.expires_at > func.now()
```

Теперь истёкшие офферы **автоматически исчезают** из списка "Новые заказы" мастера.

### FIX 3: Улучшенное логирование
Добавлены детальные логи:
```python
_log.info("offer_accept: offer expired by time for order=%s master=%s (expires_at=%s)", 
         order_id, master.id, expires_at.isoformat())
```

## 🧪 Тест-кейс

### Подготовка:
```sql
-- 1. Убедись что мастер на смене
UPDATE masters SET is_on_shift = true WHERE id = 86;

-- 2. Создай заказ с коротким SLA для теста (30 секунд)
INSERT INTO orders (city_id, district_id, category, status, timeslot_start_utc, timeslot_end_utc)
VALUES (1, 2333, 'HANDYMAN', 'SEARCHING', NOW() + INTERVAL '1 hour', NOW() + INTERVAL '2 hours')
RETURNING id;

-- Предположим получили id=14
```

### Тестирование через админ-бота:
1. **Создай заказ:** Быстрое создание → город Москва → район ЮЗАО → Сегодня → 10-13 → категория "Мелкий ремонт"
2. **Запусти автораспределение:** Заявки → Очередь → Заказ #14 → Автораспределение → Да
3. **Мастер получает оффер** в боте (проверь что пришёл)
4. **Жди 2 минуты** (SLA 120 секунд истекает)
5. **Мастер пытается нажать "Взять"** → должен получить: ⏰ "Время истекло..."
6. **Обнови список** "Новые заказы" → заказ **должен исчезнуть**

### Проверка в логах:
```bash
# Мастер-бот
docker-compose logs -f --tail=100 master-bot | grep "offer_accept"

# Ожидаемое:
# offer_accept: offer expired by time for order=14 master=86 (expires_at=2025-10-10T...)
```

### Проверка в БД:
```sql
-- Оффер должен быть в состоянии EXPIRED или остаться SENT (если не истёк вручную)
SELECT id, order_id, master_id, state, expires_at, NOW() as current_time,
       expires_at < NOW() as is_expired
FROM offers 
WHERE order_id = 14;

-- Заказ должен быть либо SEARCHING (если раунды не закончились)
-- либо эскалирован к логисту
SELECT id, status, dist_escalated_logist_at, dist_escalated_admin_at
FROM orders 
WHERE id = 14;
```

## 🚀 Применение

### 1. Перезапуск мастер-бота
```powershell
cd C:\ProjectF\field-service
docker-compose restart master-bot
docker-compose logs -f --tail=100 master-bot
```

### 2. Обновление snapshot (после проверки)
```powershell
python scripts/create_snapshot.py
```

## 📊 Ожидаемые результаты

### ДО исправления ❌:
- Мастер видит истёкшие офферы в списке
- При нажатии "Взять" получает "Заказ уже занят"
- Заказ эскалируется многократно
- Путаница и плохой UX

### ПОСЛЕ исправления ✅:
- Истёкшие офферы автоматически скрыты
- При попытке принять (если успел открыть до истечения): ⏰ "Время истекло..."
- Одна эскалация после исчерпания раундов
- Понятные сообщения об ошибках

## 🔍 Дополнительная проверка

### Сценарий 1: Оффер истёк, но мастер ещё не обновил список
```
1. Мастер открыл "Новые заказы" в 23:31
2. Время истекло в 23:33
3. Мастер нажимает на заказ в 23:35
4. Результат: ⏰ "Время истекло..." ✅
```

### Сценарий 2: Мастер обновил список после истечения
```
1. Время истекло в 23:33
2. Мастер открывает "Новые заказы" в 23:35
3. Результат: Заказа НЕТ в списке ✅
```

### Сценарий 3: Мастер отклонил, потом пытается принять
```
1. Мастер нажал "Отказаться" → state=DECLINED
2. Мастер через API пытается принять (callback hack)
3. Результат: ❌ "Вы уже отклонили этот заказ" ✅
```

## 🎯 Связанные проблемы решены

1. ✅ Дублирующиеся эскалации в логах
2. ✅ Непонятные сообщения мастерам
3. ✅ Заказы "зависали" в SEARCHING
4. ✅ Race condition между мастером и автораспределением

---

**Статус:** ✅ READY FOR TESTING  
**Автор:** Claude Sonnet 4.5  
**Дата:** 2025-10-10
