# 🎯 ИНСТРУКЦИЯ ПО ПРИМЕНЕНИЮ ОСТАВШИХСЯ ПАТЧЕЙ

## ✅ Что уже сделано автоматически:

1. ✅ **P1-01: Автозакрытие** - полностью применён
2. ✅ **P1-03: Слоты** - полностью применён

---

## 📝 P1-06: РЕФЕРАЛКА v2 - АДМИН UI

### Шаг 1: Добавить метод в DBMastersService

**Файл:** `field-service/field_service/bots/admin_bot/services_db.py`

**Где:** В класс `DBMastersService`, после метода `_log_admin_action()`

**Добавить:**

```python
async def get_master_referral_stats(self, master_id: int) -> dict:
    """Получить статистику по рефералам мастера."""
    async with self._session_factory() as session:
        from decimal import Decimal
        
        # Приглашённые мастера
        invited_stmt = select(func.count()).where(
            m.masters.referred_by_master_id == master_id
        )
        invited_total = int((await session.execute(invited_stmt)).scalar_one() or 0)
        
        # Ожидающие модерации
        pending_stmt = select(func.count()).where(
            m.masters.referred_by_master_id == master_id,
            m.masters.verified == False,
        )
        invited_pending = int((await session.execute(pending_stmt)).scalar_one() or 0)
        
        # Общая сумма начислений
        rewards_stmt = select(
            func.count(),
            func.sum(m.referral_rewards.amount)
        ).where(
            m.referral_rewards.referrer_id == master_id,
            m.referral_rewards.status != m.ReferralRewardStatus.CANCELED,
        )
        result = (await session.execute(rewards_stmt)).first()
        rewards_count = int(result[0] or 0)
        rewards_amount = Decimal(result[1] or 0)
        
        return {
            'invited_total': invited_total,
            'invited_pending': invited_pending,
            'rewards_count': rewards_count,
            'rewards_amount': rewards_amount,
        }
```

### Шаг 2: Обновить render_master_card

**Файл:** `field-service/field_service/bots/admin_bot/routers/admin_masters.py`

**Где:** В функции `render_master_card()`, после строки с документами (где-то около `lines.append(...)`)

**Добавить:**

```python
# P1-06: Реферальная информация
if detail.referral_code:
    lines.append(f"🎁 Реф. код: {detail.referral_code}")
    try:
        ref_stats = await service.get_master_referral_stats(master_id)
        if ref_stats['invited_total'] > 0:
            lines.append(
                f"👥 Рефералов: {ref_stats['invited_total']} "
                f"(ожидают: {ref_stats['invited_pending']})"
            )
            lines.append(
                f"💰 Начислено: {ref_stats['rewards_amount']:.2f} ₽ "
                f"({ref_stats['rewards_count']} шт.)"
            )
    except Exception:
        logger.debug("Failed to load referral stats", exc_info=True)
```

---

## 🔔 P1-05: PUSH-УВЕДОМЛЕНИЯ

### Шаг 1: Обновить admin_bot/routers/admin_masters.py

**Найти:** Любые вызовы `notify_master()` или прямые отправки сообщений мастерам

**Заменить на:**

```python
from field_service.services.push_notifications import notify_master, NotificationEvent

async with session_factory() as session:
    await notify_master(
        session,
        master_id=master_id,
        event=NotificationEvent.MODERATION_APPROVED,
    )
    await session.commit()
```

### Шаг 2: Обновить watchdogs.py

**Файл:** `field-service/field_service/services/watchdogs.py`

**В функции `_notify_master_blocked`, заменить:**

```python
from field_service.services.push_notifications import notify_master, NotificationEvent

await notify_master(
    session,
    master_id=event.master_id,
    event=NotificationEvent.ACCOUNT_BLOCKED,
    reason="Просрочка оплаты комиссии",
)
```

**Добавить уведомление админов:**

```python
from field_service.services.push_notifications import notify_admin, NotificationEvent

await notify_admin(
    bot,
    alerts_chat_id,
    event=NotificationEvent.COMMISSION_OVERDUE,
    commission_id=event.commission_id,
    order_id=event.order_id,
    master_id=event.master_id,
    master_name=event.master_full_name or f"Мастер #{event.master_id}",
)
```

### Шаг 3: Обновить distribution_worker.py

**При эскалации логисту добавить:**

```python
from field_service.services.push_notifications import notify_admin, NotificationEvent
from field_service.config import settings as env_settings

if env_settings.alerts_chat_id:
    await notify_admin(
        bot,
        env_settings.alerts_chat_id,
        event=NotificationEvent.ESCALATION_LOGIST,
        order_id=order_id,
    )
```

**При эскалации админу:**

```python
if env_settings.alerts_chat_id:
    await notify_admin(
        bot,
        env_settings.alerts_chat_id,
        event=NotificationEvent.ESCALATION_ADMIN,
        order_id=order_id,
    )
```

---

## 🚀 ПРИМЕНЕНИЕ ИЗМЕНЕНИЙ

### 1. Миграция БД (ОБЯЗАТЕЛЬНО!)

```bash
cd field-service
alembic upgrade head
```

**Проверка:**
```sql
SELECT * FROM order_autoclose_queue LIMIT 1;
```

### 2. Перезапуск сервисов

```bash
# Если используете systemd:
sudo systemctl restart field-service-admin-bot
sudo systemctl restart field-service-master-bot

# Или через supervisor:
supervisorctl restart field-service-admin-bot
supervisorctl restart field-service-master-bot

# Или вручную:
pkill -f "admin_bot"
pkill -f "master_bot"
python -m field_service.bots.admin_bot.main &
python -m field_service.bots.master_bot.main &
```

### 3. Проверка логов

```bash
# Проверить автозакрытие
tail -f logs/admin_bot.log | grep -i autoclose

# Проверить ошибки
tail -f logs/*.log | grep -i error
```

### 4. Тестирование

**Автозакрытие:**
1. Закройте гарантийный заказ через мастер-бота
2. Проверьте: `SELECT * FROM order_autoclose_queue WHERE order_id = <ID>;`
3. Подождите 24ч или измените `autoclose_at` вручную
4. Проверьте изменение статуса заказа

**Слоты:**
1. Попробуйте создать заказ с разными слотами
2. Проверьте корректность времени

**Рефералка (после применения патча):**
1. Откройте карточку мастера с реферальным кодом
2. Убедитесь, что отображается статистика

---

## ⚠️ ROLLBACK PLAN

Если что-то пошло не так:

```bash
# 1. Откатить миграцию
cd field-service
alembic downgrade -1

# 2. Откатить изменения в коде
git checkout HEAD -- field_service/bots/master_bot/handlers/orders.py
git checkout HEAD -- field_service/bots/admin_bot/main.py
git checkout HEAD -- field_service/services/time_service.py

# 3. Перезапустить
sudo systemctl restart field-service-*
```

---

## 📊 ОЦЕНКА ВРЕМЕНИ

| Задача | Время |
|--------|-------|
| Применение P1-06 (рефералка) | 15 мин |
| Применение P1-05 (уведомления) | 30 мин |
| Миграция и перезапуск | 10 мин |
| Тестирование | 20 мин |
| **ИТОГО** | **~1 час 15 мин** |

---

## ✅ ЧЕКЛИСТ ПРИМЕНЕНИЯ

- [ ] Создал backup БД
- [ ] Применил миграцию `alembic upgrade head`
- [ ] Добавил метод `get_master_referral_stats()` в `services_db.py`
- [ ] Обновил `render_master_card()` с реферальной информацией
- [ ] Интегрировал `push_notifications` в нужные места
- [ ] Перезапустил оба бота
- [ ] Проверил логи на ошибки
- [ ] Протестировал автозакрытие
- [ ] Протестировал слоты
- [ ] Протестировал рефералку в UI

---

**Удачи! 🚀**

Все файлы уже изменены автоматически там, где это возможно.
Осталось только добавить 2 метода вручную и интегрировать уведомления.
