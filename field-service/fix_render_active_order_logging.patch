# Патч для добавления логирования в _render_active_order
# Файл: field_service/bots/master_bot/handlers/orders.py

# Цель: Понять почему заказ не отображается в "Активные заказы" после принятия

Изменения:
1. Добавлено логирование в начало _render_active_order
2. Добавлено логирование при загрузке списка активных заказов
3. Добавлено логирование при загрузке конкретного заказа
4. Добавлено логирование перед/после отправки сообщения

Проблема:
- Мастер принимает заказ (offer_accept успешно коммитится)
- Но заказ не появляется в "Активные заказы"
- В логах нет вызова _render_active_order после commit

Файл уже обновлён локально в:
C:\ProjectF\field-service\field_service\bots\master_bot\handlers\orders.py

ИНСТРУКЦИЯ ПО ПРИМЕНЕНИЮ:
1. Скопировать обновлённый файл на сервер через WinSCP
2. Перезапустить master-bot: docker restart field-service-master-bot-1
3. Принять заказ и проверить логи: docker logs -f field-service-master-bot-1
