# CHANGELOG - Гибкие перерывы

## [Unreleased] - 2025-10-10

### ✨ Добавлено

#### Гибкая длительность перерывов
- **Выбор длительности**: Мастер теперь может выбрать длительность перерыва при его начале:
  - ☕ 15 минут - короткий перерыв на кофе
  - 🍔 1 час - обеденный перерыв
  - 😴 2 часа - длинный перерыв
  
- **Гибкое продление**: При продлении перерыва также доступен выбор длительности (15 мин / 1 ч / 2 ч)

#### Таймер перерыва в главном меню
- В главном меню отображается информация о текущем перерыве:
  - Время окончания перерыва (HH:MM)
  - Оставшееся время (X ч Y мин или X мин)
  - Пример: `⏰ Перерыв до 14:30 (осталось 45 мин)`

#### Автоматическое снятие со смены
- Новый watchdog `watchdog_expired_breaks`:
  - Проверяет мастеров на перерыве каждые 60 секунд
  - Автоматически снимает со смены после истечения времени перерыва
  - Устанавливает: `shift_status = SHIFT_OFF`, `is_on_shift = false`, `break_until = NULL`
  - Мастер видит меню "вне смены" и должен явно включить смену снова

#### Обновлённые уведомления
- Напоминание за 10 минут до окончания перерыва теперь предлагает:
  - 🟢 Вернуться на смену
  - ☕ Продлить перерыв (с выбором длительности)

### 🔧 Изменено

#### Клавиатуры
- `break_duration_keyboard(extend_mode: bool)`: Новая клавиатура выбора длительности
  - `extend_mode=False`: для начала перерыва
  - `extend_mode=True`: для продления перерыва
  
- `break_reminder_keyboard()`: Обновлена для поддержки гибкого продления

#### Тексты сообщений
- `SHIFT_MESSAGES["break_choose"]`: "☕ Выберите длительность перерыва:"
- `SHIFT_MESSAGES["break_started"]`: "Перерыв на {duration} начат."
- `SHIFT_MESSAGES["break_extend_choose"]`: "☕ На сколько продлить перерыв?"
- `SHIFT_MESSAGES["break_extended"]`: "Перерыв продлён на {duration}."

#### Обработчики
- `shift_break_choose()`: Новый обработчик для показа выбора длительности
- `shift_break_start()`: Обновлён для приёма параметра длительности
- `shift_break_extend_apply()`: Новый обработчик продления с выбором

#### Главное меню
- `_render_start()`: Добавлено отображение таймера перерыва
- `_format_break_time_left()`: Новая функция форматирования времени
- `handle_cancel_callback()`, `handle_menu()`: Обновлены для показа таймера

#### Master-bot запуск
- Добавлена задача `expired_breaks_task` с `watchdog_expired_breaks()`
- Graceful shutdown с отменой новой задачи в блоке finally

### 🧪 Тестирование

#### Новые тесты
- `test_watchdog_expired_breaks_basic()`: Базовое снятие со смены
- `test_watchdog_expired_breaks_not_expired()`: Проверка что активные перерывы не трогаются
- `test_watchdog_expired_breaks_multiple_masters()`: Обработка нескольких мастеров
- `test_watchdog_expired_breaks_edge_case_exactly_now()`: Граничный случай

### 📝 Документация

#### Новые файлы
- `FLEXIBLE_BREAKS_DEPLOYMENT.md`: Полная документация по развёртыванию
  - Обзор изменений
  - Инструкции по деплою
  - Архитектура решения
  - Сценарии тестирования
  - Известные ограничения
  
- `deploy_flexible_breaks.ps1`: Автоматизированный скрипт деплоя
  - Проверка файлов
  - Запуск тестов
  - Создание бэкапа
  - Копирование на сервер
  - Перезапуск master-bot
  - Проверка логов

### 🐛 Исправлено

- Зафиксированная длительность перерыва 2 часа (теперь гибкая)
- Отсутствие таймера перерыва (теперь отображается в меню)
- Мастер оставался на перерыве после истечения времени (теперь автоснятие)

### ⚠️ Технические детали

#### Новые константы
```python
BREAK_DURATIONS = {
    "15m": timedelta(minutes=15),
    "1h": timedelta(hours=1),
    "2h": timedelta(hours=2),
}
```

#### Новые callback_data паттерны
- Начало перерыва: `m:sh:brk:15m`, `m:sh:brk:1h`, `m:sh:brk:2h`
- Продление: `m:sh:brk:ext:15m`, `m:sh:brk:ext:1h`, `m:sh:brk:ext:2h`

#### Watchdog параметры
- Интервал проверки: 60 секунд
- Использует SQL UPDATE для эффективности
- Логирование в `live_log` и стандартный logger

### 📊 Производительность

- Watchdog выполняет одиночный SQL UPDATE каждые 60 секунд
- Минимальная нагрузка на БД благодаря индексам
- Таймер не создаёт дополнительных запросов (обновляется при возврате в меню)

### 🔒 Безопасность

- Все временные операции используют UTC
- Проверки валидации длительности перерыва
- Graceful shutdown предотвращает потерю данных

### 📈 Метрики

После развёртывания можно отслеживать:
- Количество мастеров на перерыве: `SELECT COUNT(*) FROM masters WHERE shift_status = 'BREAK'`
- Срабатывания watchdog: `SELECT * FROM live_log WHERE subsystem = 'watchdog' AND text LIKE '%break_expired%'`
- Статистику длительности перерывов (можно добавить в будущем)

---

## Инструкции по развёртыванию

```powershell
# Автоматический деплой
.\deploy_flexible_breaks.ps1

# Или вручную по инструкции
# См. FLEXIBLE_BREAKS_DEPLOYMENT.md
```

---

## Обратная совместимость

✅ **Полностью совместимо** - никаких breaking changes:
- Все существующие поля БД используются
- Миграции не требуются
- Старые перерывы (если есть) обработаются корректно
- API не изменился

---

## Следующие шаги (возможные улучшения)

1. **Локализация времени** - отображение в часовом поясе города мастера
2. **Автообновление таймера** - периодическая отправка обновлений (может спамить)
3. **Статистика перерывов** - аналитика длительности и частоты
4. **Кастомная длительность** - ввод произвольного времени (15-120 минут)
5. **Push-уведомления** - уведомление об окончании перерыва через Telegram API

---

**Дата**: 2025-10-10  
**Версия**: 1.0  
**Статус**: ✅ Готово к развёртыванию
