[pytest]
# Pytest configuration for E2E tests

# Test discovery
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Test directories
testpaths = tests/e2e

# Markers
markers =
    e2e: End-to-end tests with full flow
    critical: Critical tests that must pass (run in CI/CD)
    slow: Tests that take more than 30 seconds
    warranty: Warranty-related scenarios
    finance: Financial calculations and transactions
    autoassign: Auto-assignment logic tests
    admin: Admin bot functionality
    integration: Integration tests with database

# Async configuration
asyncio_mode = auto

# Output configuration
console_output_style = progress
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)8s] %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S

# Coverage configuration
[coverage:run]
source = .
omit = 
    */tests/*
    */venv/*
    */__pycache__/*
    */conftest.py

[coverage:report]
precision = 2
show_missing = True
skip_covered = False

[coverage:html]
directory = htmlcov
