# ✅ ЧЕКЛИСТ ТЕСТИРОВАНИЯ ПАТЧЕЙ P1

**Дата создания:** 2025-10-03  
**Статус миграции:** ✅ Применена (`alembic upgrade head`)  
**Статус кода:** ✅ Все патчи применены

---

## 🎯 P1-01: АВТОЗАКРЫТИЕ ЗАКАЗОВ

### Подготовка
```sql
-- Проверить наличие таблицы
SELECT * FROM order_autoclose_queue LIMIT 1;

-- Очистить тестовые данные (если нужно)
DELETE FROM order_autoclose_queue WHERE order_id = <TEST_ID>;
```

### Тест 1: Постановка в очередь

**Шаги:**
1. Открыть мастер-бота
2. Принять гарантийный заказ
3. Перевести в статус WORKING
4. Закрыть заказ (загрузить акт)

**Ожидаемый результат:**
```sql
SELECT * FROM order_autoclose_queue WHERE order_id = <ID>;
-- Должна быть запись с:
-- - closed_at = текущее время
-- - autoclose_at = closed_at + 24 часа
-- - processed_at = NULL
```

**Статус:** [ ] PASS / [ ] FAIL

---

### Тест 2: Автозакрытие через 24ч

**Шаги:**
1. Найти заказ в очереди
2. Изменить `autoclose_at` на текущее время минус 1 минута:
```sql
UPDATE order_autoclose_queue 
SET autoclose_at = NOW() - INTERVAL '1 minute'
WHERE order_id = <ID>;
```
3. Подождать 1 цикл планировщика (по умолчанию 1 час, но можно перезапустить бота)

**Ожидаемый результат:**
```sql
-- 1. Запись помечена как обработанная
SELECT processed_at FROM order_autoclose_queue WHERE order_id = <ID>;
-- processed_at должно быть заполнено

-- 2. Статус заказа изменён на CLOSED
SELECT status FROM orders WHERE id = <ID>;
-- status = 'CLOSED'
```

**Статус:** [ ] PASS / [ ] FAIL

---

### Тест 3: Проверка логов

**Команда:**
```bash
grep -i "autoclose" logs/admin_bot.log | tail -20
```

**Ожидаемые записи:**
```
autoclose_scheduler started interval=3600s
order#<ID> auto-closed after 24h
```

**Статус:** [ ] PASS / [ ] FAIL

---

## 🎯 P1-03: НАСТРАИВАЕМЫЕ СЛОТЫ

### Тест 1: Дефолтные слоты

**Шаги:**
1. Создать заказ через админ-бота
2. Выбрать слот "Сегодня 10-13"

**Ожидаемый результат:**
```sql
SELECT timeslot_start_utc, timeslot_end_utc FROM orders WHERE id = <ID>;
-- start = сегодня 10:00 (в UTC)
-- end = сегодня 13:00 (в UTC)
```

**Статус:** [ ] PASS / [ ] FAIL

---

### Тест 2: Кастомные слоты через .env

**Шаги:**
1. Добавить в `.env`:
```bash
TIMESLOT_BUCKETS='[{"key":"09-12","start":"09:00","end":"12:00"},{"key":"12-15","start":"12:00","end":"15:00"}]'
```
2. Перезапустить боты
3. Создать заказ с новым слотом

**Ожидаемый результат:**
- Новые слоты доступны в UI
- Время корректно сохраняется в БД

**Статус:** [ ] PASS / [ ] FAIL

---

## 🎯 P1-05: PUSH-УВЕДОМЛЕНИЯ

### Тест 1: Одобрение мастера

**Шаги:**
1. Открыть админ-бота
2. Перейти в модерацию
3. Одобрить мастера

**Ожидаемый результат:**
```bash
# Проверить логи
grep -i "MODERATION_APPROVED" logs/admin_bot.log
```

Мастер получает уведомление "Анкета одобрена. Вам доступна смена."

**Статус:** [ ] PASS / [ ] FAIL

---

### Тест 2: Блокировка мастера

**Шаги:**
1. Заблокировать мастера через админ-бота
2. Указать причину

**Ожидаемый результат:**
```bash
grep -i "ACCOUNT_BLOCKED" logs/admin_bot.log
```

Мастер получает уведомление с причиной блокировки.

**Статус:** [ ] PASS / [ ] FAIL

---

### Тест 3: Эскалация логисту

**Шаги:**
1. Создать заказ без района
2. Попытаться автораспределение

**Ожидаемый результат:**
```bash
grep -i "ESCALATION_LOGIST" logs/admin_bot.log
```

В канал логистов приходит уведомление о нераспределённом заказе.

**Статус:** [ ] PASS / [ ] FAIL

---

### Тест 4: Мониторинг нераспределённых заказов

**Проверка:**
```bash
# Убедиться что задача запущена
ps aux | grep unassigned_monitor

# Проверить логи
grep -i "unassigned_monitor" logs/admin_bot.log
```

**Ожидаемые записи:**
```
unassigned_monitor started interval=600s
Found <N> unassigned orders older than 10 minutes
```

**Статус:** [ ] PASS / [ ] FAIL

---

## 🎯 P1-06: РЕФЕРАЛКА В АДМИНКЕ

### Тест 1: Карточка мастера с рефералами

**Шаги:**
1. Найти мастера с `referral_code`
2. Открыть его карточку в админ-боте

**Ожидаемый результат:**

Карточка содержит:
```
🎁 Реф. код: ABC123
👥 Рефералов: 5 (ожидают: 2)
💰 Начислено: 12500.00 ₽ (25 шт.)
```

**Статус:** [ ] PASS / [ ] FAIL

---

### Тест 2: Мастер без рефералов

**Шаги:**
1. Открыть карточку мастера без `referral_code`

**Ожидаемый результат:**
- Реферальная информация не отображается
- Никаких ошибок в логах

**Статус:** [ ] PASS / [ ] FAIL

---

### Тест 3: Проверка статистики

**Шаги:**
```sql
-- Создать тестовые данные
INSERT INTO masters (full_name, referred_by_master_id, verified, ...) 
VALUES ('Test Referral', <REFERRER_ID>, FALSE, ...);

INSERT INTO referral_rewards (referrer_id, amount, status, ...) 
VALUES (<REFERRER_ID>, 500.00, 'ACCRUED', ...);
```

2. Обновить карточку мастера-реферера

**Ожидаемый результат:**
- Счётчики обновились
- Суммы корректны

**Статус:** [ ] PASS / [ ] FAIL

---

## 🚨 КРИТИЧНЫЕ ПРОВЕРКИ

### ✅ Checklist перед production

- [ ] Миграция БД применена без ошибок
- [ ] Боты запускаются без ошибок
- [ ] Нет ошибок в логах при запуске
- [ ] Планировщик автозакрытия запущен
- [ ] Мониторинг нераспределённых заказов работает
- [ ] Все 4 теста автозакрытия прошли
- [ ] Все 2 теста слотов прошли
- [ ] Все 4 теста уведомлений прошли
- [ ] Все 3 теста рефералки прошли

---

## 📊 РЕЗУЛЬТАТЫ

**Дата тестирования:** _________________

### Итоги по модулям:

| Модуль | Тестов | Пройдено | Провалено |
|--------|--------|----------|-----------|
| P1-01: Автозакрытие | 3 | ___ | ___ |
| P1-03: Слоты | 2 | ___ | ___ |
| P1-05: Уведомления | 4 | ___ | ___ |
| P1-06: Рефералка | 3 | ___ | ___ |
| **ИТОГО** | **12** | ___ | ___ |

### Критичные баги:

1. ________________________________________________
2. ________________________________________________
3. ________________________________________________

### Некритичные замечания:

1. ________________________________________________
2. ________________________________________________
3. ________________________________________________

---

## 🔧 ROLLBACK PLAN

Если тестирование провалилось:

```bash
# 1. Остановить боты
sudo systemctl stop field-service-*

# 2. Откатить миграцию
cd field-service
alembic downgrade -1

# 3. Откатить код
git stash  # или git checkout <commit>

# 4. Перезапустить
sudo systemctl start field-service-*
```

---

## 📞 КОНТАКТЫ

**Разработчик:** Claude (AI Assistant)  
**Дата релиза:** 2025-10-03  
**Версия:** P1 Full Stack

**При обнаружении багов:**
1. Скопировать лог-файлы
2. Сделать дамп БД проблемного участка
3. Описать шаги воспроизведения
